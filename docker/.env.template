# ==============================================================================
# CompliPay Environment Configuration Template
# ==============================================================================
# For TaxFly Partnership Deployment
#
# Instructions:
# 1. Copy this file to .env in the same directory as docker-compose.yml
# 2. Replace all placeholder values (marked with <...>)
# 3. Generate a secure APP_KEY using: php artisan key:generate --show
# 4. Run: docker-compose up -d
# ==============================================================================

# ------------------------------------------------------------------------------
# Application Settings
# ------------------------------------------------------------------------------
APP_NAME=CompliPay
APP_ENV=production
APP_KEY=<GENERATE_WITH_php_artisan_key:generate>
APP_DEBUG=false
APP_URL=https://<YOUR_DOMAIN>

# Image tag for deployment
IMAGE_TAG=latest
APP_PORT=8000

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=complipay
DB_USERNAME=complipay
DB_PASSWORD=<SECURE_DATABASE_PASSWORD>
DB_ROOT_PASSWORD=<SECURE_ROOT_PASSWORD>

# External port mapping (optional - for direct DB access)
DB_EXTERNAL_PORT=3306

# ------------------------------------------------------------------------------
# Redis Configuration
# ------------------------------------------------------------------------------
REDIS_HOST=redis
REDIS_PASSWORD=<SECURE_REDIS_PASSWORD>
REDIS_PORT=6379
REDIS_EXTERNAL_PORT=6379

# ------------------------------------------------------------------------------
# Cache, Session & Queue
# ------------------------------------------------------------------------------
CACHE_DRIVER=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis

# ------------------------------------------------------------------------------
# ZATCA Integration
# ------------------------------------------------------------------------------
# Environment: sandbox, simulation, production
ZATCA_ENVIRONMENT=sandbox

# ZATCA API credentials (from Fatoora Portal)
ZATCA_USERNAME=<ZATCA_API_USERNAME>
ZATCA_PASSWORD=<ZATCA_API_PASSWORD>

# ------------------------------------------------------------------------------
# Logging
# ------------------------------------------------------------------------------
LOG_CHANNEL=stack
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# Database Migration
# ------------------------------------------------------------------------------
# Set to true only for initial deployment or updates
# CAUTION: Always backup database before enabling
AUTO_MIGRATE=false

# ------------------------------------------------------------------------------
# SSL/TLS (for Traefik reverse proxy)
# ------------------------------------------------------------------------------
APP_DOMAIN=<YOUR_DOMAIN>
SSL_EMAIL=<YOUR_EMAIL_FOR_LETSENCRYPT>

# ------------------------------------------------------------------------------
# SECURITY NOTES
# ------------------------------------------------------------------------------
# 1. Never commit this file to version control
# 2. Use strong, unique passwords for all credentials
# 3. Rotate passwords periodically
# 4. Restrict database access to container network only
# 5. Enable SSL/TLS for all external connections
# 6. Keep this file with restricted permissions (chmod 600)
# ==============================================================================
